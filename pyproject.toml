[project]
name = "nexus-audit"
version = "0.1.0"
dependencies = ["click", "prompt-toolkit"]

[project.scripts]
nexus = "cli:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["core*", "api*", "plugins*", "frontend*"]

[tool.setuptools]
py-modules = ["cli", "orchestrator"]
